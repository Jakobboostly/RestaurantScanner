export class SeleniumScreenshotService {
  async captureGoogleSearch(query: string, location: string) {
    return { screenshot: null, success: false };
  }

  async captureScreenshot(url: string) {
    return { screenshot: null, success: false };
  }
}